import { ProdInfoFromIdPipe } from './prod-info-from-id.pipe';

describe('ProdInfoFromIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ProdInfoFromIdPipe();
    expect(pipe).toBeTruthy();
  });
});
